# Dataset

The original `candidates.jsonl` dataset is **not included** in this repository because it exceeds GitHub's 100 MB file size limit.

Place the dataset in this folder before running the project:

```text
data/candidates.jsonl
```

After adding the dataset, run:

```bash
python src/ranker.py
```
